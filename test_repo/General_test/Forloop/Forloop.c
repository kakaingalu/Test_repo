#include <stdio.h>

/**
 * main - prints hello world and the oucome of for loop and a new line.
 *
 * Return: Always 0.
 */
int main(void)
{
	for (int i = 1; i < 11; i++)
	{
		printf("Hello, World!\n");
	}
	return (0);
}
